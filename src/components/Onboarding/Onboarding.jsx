import React from 'react';
import ChatOnboarding from './ChatOnboarding';

// Componente de Onboarding usando a interface de chat
const Onboarding = () => {
  return <ChatOnboarding />;
};

export default Onboarding; 